name: CI

on:
  push:
  pull_request:

jobs:
  gate:
    runs-on: ubuntu-latest
    env:
      VISUAL_CI_ENABLED: "false"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run gate
        run: pnpm ci:gate

      - name: Install Playwright browser
        if: ${{ env.VISUAL_CI_ENABLED == 'true' }}
        run: pnpm visual:install-browsers

      - name: Capture current screenshots
        if: ${{ env.VISUAL_CI_ENABLED == 'true' }}
        run: pnpm visual:capture:current

      - name: Visual regression (block mode)
        if: ${{ env.VISUAL_CI_ENABLED == 'true' }}
        run: VISUAL_GATE_MODE=block pnpm visual:regression
