# Review Artifacts

All non-trivial code changes must leave two review artifacts:

1. `checklists/*.md`: execution checklist for this change.
2. `records/*.md`: key implementation details and verification evidence.

Use `pnpm review:artifacts` to validate required sections before merge.
