{
  "intentIR": {
    "intentId": "intent-skill-case-4",
    "page": "page-4",
    "actors": ["operator"],
    "actions": ["open", "save"],
    "rules": [
      {
        "id": "R4",
        "when": "save.click",
        "then": "persist data"
      }
    ],
    "states": ["loading", "ready"],
    "api": {
      "method": "POST",
      "endpoint": "/api/case-4"
    },
    "unknowns": []
  },
  "pageSpec": {
    "id": "page-4",
    "route": "/case-4",
    "datasource": {
      "api": "GET /api/case-4",
      "query": ["id"]
    },
    "permissions": {
      "view": "case:view"
    },
    "ui": {
      "type": "form"
    },
    "states": ["loading", "ready"],
    "acceptance": ["renders"],
    "unknowns": []
  },
  "contractSpec": {
    "featureId": "feature-case-4",
    "entities": {
      "record": {
        "id": "string"
      }
    },
    "transitions": [
      {
        "id": "T4",
        "when": "save.click",
        "onConfirm": ["save"],
        "onCancel": ["no-op"],
        "sourceScope": "SERVICE_INFO"
      }
    ],
    "invariants": ["id unique"],
    "unknowns": []
  },
  "evalSpec": {
    "scope": "scope-case-4",
    "commands": ["pnpm spec:validate"],
    "thresholds": {
      "maxDiffPixelRatio": 0.003
    },
    "blockRules": ["any_failed_command"],
    "requiredReports": ["manifest"],
    "testCases": [
      {
        "id": "TC-4",
        "given": "state ready",
        "when": "run",
        "then": "pass"
      }
    ],
    "unknowns": []
  }
}
