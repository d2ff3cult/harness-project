{
  "evalSpec": {
    "scope": "eval-gate-case-7-source-isolation",
    "commands": ["pnpm spec:validate", "pnpm design:validate"],
    "thresholds": {
      "maxDiffPixelRatio": 0.003,
      "unknowns": 0
    },
    "blockRules": ["any_failed_command", "unknowns_non_empty"],
    "requiredReports": ["manifest", "eval-report"],
    "testCases": [
      {
        "id": "E-7",
        "given": "transport-origin record add/delete operation",
        "when": "save succeeds",
        "then": "service selections remain unchanged",
        "tags": ["source-isolation-pass", "source-isolation-fail"]
      }
    ],
    "unknowns": []
  },
  "gateConfig": {
    "strict": true
  }
}
